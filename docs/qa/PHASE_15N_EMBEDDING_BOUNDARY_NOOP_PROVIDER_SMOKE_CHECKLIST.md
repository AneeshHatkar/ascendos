# Phase 15N — Embedding Boundary / Noop Provider Smoke Checklist

## Required checks

- [ ] Embedding Boundary / Noop Provider helper exists.
- [ ] embedding boundary marker is visible.
- [ ] noop provider marker is visible.
- [ ] disabled by design marker is visible.
- [ ] no embeddings generated marker is visible.
- [ ] no provider calls marker is visible.
- [ ] no vector search marker is visible.
- [ ] no pgvector marker is visible.
- [ ] no SQL reads or writes marker is visible.
- [ ] no Supabase calls marker is visible.
- [ ] no hidden Carnos prompt injection marker is visible.
- [ ] standalone /memory route remains absent marker is visible.
- [ ] `/knowledge` renders the Phase 15N panel.
- [ ] `npm run audit:phase15n` passes.
- [ ] `npm run build` passes.

## Next phase

Phase 15O — Forget/Delete Derived Records.
